# noSQLInjection-MongoDB
create a new lab of OWASP for personal usess
for run the project 
first install nodejs
and then uses ``` npm install ``` for install all dependency and package 
and then you run the project ```  npm test ```
